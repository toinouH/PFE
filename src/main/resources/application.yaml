spring:
  threads:
    virtual:
      enabled: true
  application:
    name: railflow
  datasource:
    url: jdbc:h2:file:./data/sncfdb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

path:
  fichierReferentielGares: /path/to/dossiers/referentiel
  dossierEntreesCAV: /path/to/dossiers/entrees
  dossierCAVEnrichisTries: /path/to/dossiers/sorties

distance:
    maxkm: 500.00


